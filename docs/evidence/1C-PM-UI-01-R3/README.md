# 1C-PM-UI-01@R3 real-device visual evidence

- `ready.png`: Galaxy Watch 7, 480×480, after closing the computer picker. It is byte-identical to the frozen R2 `design/watch-ui/0.3.0-dev.1-candidate.3/locked-ready.png`.
- `settings-menu.png`: final R3 Debug APK installed with app data preserved; both settings actions, titles, icons and close control fit. Compare with the approved `design/watch-ui/0.3.0-dev.1-candidate.3/01-settings-menu.png` concept. The physical circular display masks the square screenshot's corners.
- `switch-picker.png`: pressing the top row opens the pre-existing picker, showing the current authenticated target and search in progress. This image includes a local IP address, but no token. Do not publish it unnecessarily.
- The close X returned to Ready. The second row opened the existing connection settings; no settings were edited. Its diagnostic screenshot (which included a partially masked token) was deleted locally and on the Watch rather than retained here.
- One wake-transition tap accidentally started recording; it was immediately cancelled via the existing Cancel action. No recording acceptance or new upload is claimed. The Watch was subsequently relaunched into SayIt without clearing data.
- Two-PC selection and recording-to-text are outside this local UI visual check and remain pending.
